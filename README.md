- 1️⃣ What is the difference between var, let, and const?

var: It is a golbal scope variable, it can be re-declard & upadate.

Const: Its is a block scope variable, normally we can say that it cannnot be re-declared or update.

Let: It is a block scope variable, we cannot re-declare but can can update.


- 2️⃣ What is the spread operator (...)?
  
In javascripint through spread operator we can expands more elements into an array.

- 3️⃣ What is the difference between map(), filter(), and forEach()?

map() - It is used to transform each elemetnt into a new value and it retus a new array of the same length with the transformed elements.

filter() - its using for getting elements which are meet a specific condition.

forEach() - it used to execute a funtion for each element of an array. 

- 4️⃣ What is an arrow function?

Arrow function is a shorter and clean version to write a funtion in javaScript. It was introduced in 2015 , called ES6 model.

- 5️⃣ What are template literals?

Template literals cames with ES6 model where we can use backticks ` ` insted of single (' ') or double (" ") quotes.Through this we can write whole  html code without facing problems. 


